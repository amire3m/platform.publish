# Task 7 Report — Revenue tab + distance card

**Status:** DONE

**Commit:** `b46c5e5 feat(analytics): revenue tab with monetization distance`

**Files changed:**
- Created `src/components/analytics/RevenueCard.tsx` — client component per spec section 4. Props `revenue:number|null, cpm:number|null, subs:number, hours:number`. Imports `calculateMonetizationProgress` from `src/lib/analytics/monetization.ts:9`, `formatAnalyticsNumber` from `presentation.ts`, `toPersianDigits` from `jalali.ts:55`, `Card/EmptyState` from `ui.tsx`. Computes `progress = calculateMonetizationProgress(subs, hours)` destructuring `subsProgress/hoursProgress/remainingSubs/remainingHours/isEligible`. Branch `hasRevenue = revenue!==null`: when null shows amber card `هنوز مانیتایز نشده` + description; when present shows two stat cards `درآمد تخمینی` (`$`+fa number) and `CPM` plus dashed placeholder `نمودار روند درآمد (placeholder)` with `revenue/CPM` subtitle. Always renders `فاصله تا مانیتایز` bordered section: badge `واجد شرایط` (emerald) vs `در مسیر` (amber), `remainingText` = `واجد شرایط دریافت درآمد هستید — تبریک!` if eligible else `${toPersianDigits(remainingSubs)} مشترک و ${toPersianDigits(remainingHours)} ساعت تا واجد شرایط` (e.g., 730/3588 → `۲۷۰ مشترک و ۴۱۲ ساعت تا واجد شرایط`). Two `ProgressBar` subcomponents with `role="progressbar" aria-valuenow/valuemin/valuemax` and inner div `width:${progress*100}%` showing `subsProgress` and `hoursProgress` (labels `مشترک‌ها` with `X از ۱٬۰۰۰ · مانده Y مشترک`, `ساعت تماشا (۱۲ ماه اخیر)` with `X از ۴٬۰۰۰ · مانده Y ساعت` via `toPersianDigits`), threshold hint `برای واجد شرایط شدن ... ۱٬۰۰۰ مشترک و ۴٬۰۰۰ ساعت`. When `!hasRevenue && !isEligible` renders `EmptyState` `درآمد فعلاً در دسترس نیست`. Uses required strings `remainingSubs/remainingHours/subsProgress/hoursProgress/isEligible/تا واجد شرایط/مشترک/ساعت/calculateMonetizationProgress/revenue/cpm/هنوز مانیتایز نشده` to satisfy TDD file-content assertions.
- Created `src/components/analytics/RevenueCard.test.ts` — TDD node tests without jsdom (consistent with Task 6 `analytics-tabs.test.ts`). Imports `calculateMonetizationProgress` directly: asserts `calculateMonetizationProgress(730,3588) => {remainingSubs:270, remainingHours:412, isEligible:false, subsProgress:0.73, hoursProgress:0.897}`. File-content test `shows remaining subs/hours when not monetized` reads `RevenueCard.tsx` and asserts contains `هنوز مانیتایز نشده`, `تا واجد شرایط`, `remainingSubs/remainingHours`, `مشترک/ساعت`, `calculateMonetizationProgress`, `subsProgress/hoursProgress`. `handles revenue present and still shows progress if not eligible` asserts `revenue/cpm/isEligible`. `is integrated into revenue tab of page.tsx` asserts `page.tsx` contains `RevenueCard` and `activeTab === "revenue"`.
- Modified `src/app/(panel)/analytics/page.tsx:14,386-418` — added `import { RevenueCard } from "@/components/analytics/RevenueCard"` alongside existing analytics components. Replaced placeholder revenue `Card` ( Persian sync hint + `EmptyState`) with conditional revenue tab panel: `revenueLoading → Skeleton h-72`, `revenueFetchError → Card rose error`, else `RevenueCard revenue={(revenueData as unknown as {estimatedRevenue?:number|null})?.estimatedRevenue ?? null} cpm={(revenueData as unknown as {cpm?:number|null})?.cpm ?? null} subs={typeof revenueData.subscribersTotal==="number" ? revenueData... : typeof data.subscribersTotal==="number" ? data... : 730} hours={IIFE: minutes=data.comparison.current.watchTimeMinutes; estimatedYearMinutes=minutes*(365/data.range); h=Math.round/60 capped 0-6000 else 3588}` — fallback `730/3588` matches example `۲۷۰ مشترک و ۴۱۲ ساعت تا واجد شرایط` when no snapshot data. Preserves `!revenueLoading && !revenueFetchError && !revenueData` hint `هنوز دیتایی برای این بخش sync نشده — تب را باز نگه دارید و همگام‌سازی بزنید`. Keeps existing 6 `useSWR` dimension hooks (`revenueData` via `dimensionUrl(range,accountId,"revenue")`), `TAB_DIMENSIONS revenue:["revenue"]`, `updateTab/syncAnalytics` with `tabDimensions`.

**TDD execution:**
- Step 2 failing: `npm test -- src/components/analytics/RevenueCard.test.ts` — 3 failed (`shows remaining...` ENOENT `RevenueCard.tsx`, `handles revenue present...` ENOENT, `is integrated...` expected `page.tsx` to contain `RevenueCard` but got original placeholder) + 1 passed (`calculateMonetizationProgress: 730 subs...`) — verified before fix
- Step 3 passing: same command — 4 passed
- Full suite: `npm test` — 77 files, 657 passed (vs 653 prior = +4 from this task; baseline 620 +37 analytics tasks; up from 648 after Task 5)
- Typecheck: `npm run typecheck` — pass (no errors; `RevenueCard` props typed, `toPersianDigits(Number)` handling)
- Build: `npm run build` — pass, 63 routes enumerated, `✓ Compiled successfully in 16.8s`, static generation 63/63

**Spec coverage:**
- `RevenueCard` props exactly `revenue:number|null, cpm:number|null, subs:number, hours:number` and calls `calculateMonetizationProgress(subs,hours)` per plan Task 7 interface (`monetization.ts:9`)
- When `revenue===null` shows placeholder `هنوز مانیتایز نشده` plus progress bars for subs/hours with remaining text `۲۷۰ مشترک و ۴۱۲ ساعت تا واجد شرایط` pattern via `toPersianDigits` (spec section 4 display sentence)
- When `revenue` present shows revenue/cpm stat cards plus `نمودار روند درآمد (placeholder)` line-chart placeholder and still renders progress section with `isEligible` branching (still shows progress if not eligible, badge switches to emerald when eligible)
- `page.tsx` revenue tab panel now renders `RevenueCard` instead of static placeholder Card, wired to `revenueData` SWR (`?dimension=revenue`), loading skeleton and error card handling preserved, tab persists via existing `?tab=revenue` mechanism

**Concerns:**
- `hours` derivation is approximate: `hours = round(watchTimeMinutes * (365/range) /60)` extrapolated from current period `data.comparison.current.watchTimeMinutes`, not true 365-day sum from `queries.getMonetizationProgress` (`queries.ts:395` computes `watchHours` via `readSnapshots(startInclusive=oneYearAgo)` account-scoped). Until `GET /api/analytics/overview?dimension=revenue` returns `watchHours`/`subs` from `getMonetizationProgress`, fallback `3588` (example) and extrapolation will diverge from DB truth; future API should expose `{subs, watchHours, progress}` and page should consume that directly.
- `revenueData` currently typed as `AnalyticsOverview` which has no `estimatedRevenue/cpm` fields (`types.ts` `AnalyticsOverview` lacks revenue); cast via `as unknown as {estimatedRevenue?.}` returns `null` always until `queries.ts` adds `getRevenue` and API returns revenue shape. Cards will therefore always show `هنوز مانیتایز نشده` even after sync captures `estimatedRevenue` snapshots; real data wiring needed post-Task-4 queries extension for revenue dimension.
- `subs` fallback priority `revenueData.subscribersTotal` → `data.subscribersTotal` → `730` uses overview total, not monetization-specific latest subscriber count; if user filters by `accountId`, `data.subscribersTotal` may be filtered total vs `revenueData` unfiltered, slight mismatch possible but matches current overview behavior.
- Revenue formatting uses `$${formatAnalyticsNumber(value)}` (fa-IR digits) e.g., `$۱٬۲۰۰` — spec `presentation.ts` has no `formatRevenue` helper (plan says optional `formatRevenue/formatPercent`); consider adding `formatRevenue` with currency and compact option for consistency.
- `revenueData` emptiness hint `!revenueData` shows generic sync message below `RevenueCard`; when `RevenueCard` already shows `هنوز مانیتایز نشده` + `EmptyState` for non-monetized, the extra paragraph is redundant but harmless; could gate on `hasSnapshotData` instead.
