# Task 6 Report — UI Tabs + 5 new charts/tables

**Status:** DONE

**Commit:** `8387874 feat(analytics): tabbed UI for geo/audience/traffic/search/retention`

**Files changed:**
- Modified `src/app/(panel)/analytics/page.tsx:1-200` — added 6-tab navigation (`نمای کلی | ترافیک | مخاطب | جستجو | ماندگاری | درآمد`) with `?tab=` persisted via `useSearchParams`/`router.replace`; added `TabId` union + `TABS` constant + `selectedTab()` + `dimensionUrl()` helper + `TAB_DIMENSIONS` map (`overview→undefined`, `traffic→["traffic"]`, `audience→["audience","geo","device"]`, `search→["search"]`, `retention→["retention"]`, `revenue→["revenue"]`); added 6 `useSWR` hooks each conditional on `activeTab===dimension` fetching `GET /api/analytics/overview?dimension=geo` style endpoints (`traffic`, `audience`, `geo`, `search`, `retention`, `revenue`); added `updateTab()` preserving existing `accountId/range/scope` params; extended `syncAnalytics` to compute `tabDimensions` and pass `dimensions` to `buildAnalyticsSyncRequest` + `runAnalyticsSync`, with `revalidateOverview` also revalidating active dimension URL via `mutateCache`; refactored `updateFilters` to keep `?tab=` while changing filters; moved overview `StatRail/Trend/TopVideos` into `panel-overview`tabpanel and rendered `TrafficTable`, `GeoChart+AudienceChart`, `SearchTermsTable`, `RetentionChart`, revenue placeholder card in their own `role="tabpanel"` panels; added empty-state Persian message `هنوز دیتایی برای این بخش sync نشده — تب را باز نگه دارید و همگام‌سازی بزنید` via child components, button group `role="tablist"` with `role="tab"` `aria-selected`/`aria-controls` and Tailwind active styling (`bg-tg-accent` when selected); kept `AnalyticsFilters`, `SyncResults`, error banners above tabs
- Created `src/components/analytics/GeoChart.tsx` — client bar chart by country (Top 10) with `recharts` `BarChart`, `PLACEHOLDER_DATA` (IR/US/DE/TR/GB), `EMPTY_MESSAGE` dashed box + `EmptyState` when `data` empty, `isLoading→Skeleton`, `error→Card` rose
- Created `src/components/analytics/AudienceChart.tsx` — two cards: stacked-ageGroup bar + device pie (`PieChart` with `PLACEHOLDER_DEVICE` MOBILE/DESKTOP/TV), same empty/loading/error pattern, reuses `EMPTY_MESSAGE`
- Created `src/components/analytics/TrafficTable.tsx` — left table `trafficSource/views/watchTime` + right donut `Pie` share, placeholder rows `YT_SEARCH/EXT_URL/RELATED_VIDEO/BROWSE`, empty dashed + `EmptyState`
- Created `src/components/analytics/SearchTermsTable.tsx` — sortable table `keyword/views/watchTime` with `ArrowUpDown` toggle, placeholder keywords `امام روح‌الله/سخنرانی/...`, empty handling
- Created `src/components/analytics/RetentionChart.tsx` — `ComposedChart` `Bar` for `averageViewPercentage` (left y 0-100٪) + `Line` for `views` (right y), `PLACEHOLDER` 4 videos, empty/loading/error identical phrasing, formatter fixed for typecheck via `as never`
- Modified `src/lib/analytics/analytics-controls.ts:9-38` — extended `buildAnalyticsSyncRequest(accountId, permissions, allowedAccountIds, dimensions?)` and `executeAnalyticsSyncRequest(..., dimensions?)` to include `dimensions` array in JSON body (`{accountId, dimensions}` for scoped sync, `{dimensions}` for bulk `body===""` case via `JSON.stringify({dimensions})` instead of `""`), kept backward compat when `dimensions` undefined/empty; guarded empty via `hasDimensions` flag
- Modified `src/lib/analytics/sync-controller.ts:27-66` — added `dimensions?: readonly string[] | null` to `AnalyticsSyncControllerOptions`, forwarded to `executeAnalyticsSyncRequest` in `runAnalyticsSync`
- Created `src/components/analytics/analytics-tabs.test.ts` — TDD file-content tests (node env, no jsdom needed): asserts `page.tsx` contains `ترافیک`, all 6 Persian labels, `searchParams.get("tab")` + `?tab=` + `role="tab"`, `dimension=` + `useSWR`, and that 5 component files exist and contain `هنوز دیتایی`

**TDD execution:**
- Step 2 failing: `npm test -- src/components/analytics/analytics-tabs.test.ts` — 5 failed (`has tab "ترافیک"`, `has all 6 Persian tab labels` missing ترافیک, `persists tab via ?tab=` missing `searchParams.get("tab")`, `uses useSWR with dimension param` missing `dimension=`, `creates 5 dimension components` ENOENT for `GeoChart.tsx`) — verified before fix
- Step 3 passing: same command — 5 passed
- Additional fix: first `npm run typecheck` failed on `RetentionChart.tsx:59` `Formatter` type mismatch for `(value:number,name:string)` with `undefined` union; fixed by casting `Number(value)` and `as never`
- Full suite: `npm test` — 76 files, 653 passed (vs 648 prior = +5 from this task, total 620 baseline +33 analytics tasks)
- Typecheck: `npm run typecheck` — pass
- Build: `npm run build` — pass, 63 routes enumerated (identical to baseline, no new route), `✓ Compiled successfully in 16.9s`

**Spec coverage:**
- 6 tabs rendered with Persian labels and `?tab=` persistence, `role="tab"` accessibility as per plan Task 6 Step 3
- Each non-overview tab has conditional `useSWR` to its dimension endpoint (`dimension=traffic/audience/geo/search/retention/revenue`), reusing existing `GET /api/analytics/overview?dimension=...` validation (added in Task 5 `ALLOWED_OVERVIEW_DIMENSIONS`)
- 5 dimension components created with at least basic rendering (bar/table/pie/composed) and placeholder + empty-state message `هنوز دیتایی برای این بخش sync نشده …` for empty sync, matching spec empty states
- Sync button now sends `dimensions` of active tab via `TAB_DIMENSIONS` through `analytics-controls` → `sync-controller` → `POST /api/analytics/sync {accountId, dimensions}` lake flow, committed in Task 5 single transaction
- Overview tab retains existing `AnalyticsStatRail/AnalyticsTrendChart/TopVideos/SyncStatus` unchanged; other tabs show placeholders until real `getGeo/getAudience/...` queries implemented in later tasks

**Concerns:**
- Dimension data currently not mapped: `page.tsx` passes `trafficData ? [] : undefined` (empty array) to `TrafficTable` etc., so even when `dimension=traffic` endpoint returns `AnalyticsOverview` (since queries not yet filtering by `scopeType`), component will still show sample placeholder + empty-state, not real rows. This satisfies Task 6 minimal requirement ("reuse existing overview data or add dimension handling later") but means no real geo/audience/traffic/search/retention rows displayed until `queries.ts` `getGeo` etc. implemented; future change should replace `[]` with transformed `trafficData` (e.g., `trafficData.chartSeries` or dedicated `trafficRows` field) and wire types
- `audience` tab currently fetches two SWRs (`audience` + `geo`) but both discards payload and shows placeholders; `AudienceChart` prop `deviceData` never fed (always `undefined` → placeholder). When device dimension added, will need third SWR for `device` and composite mapping
- `TAB_DIMENSIONS` for `audience` lists `["audience","geo","device"]`; `buildAnalyticsSyncRequest` serializes bulk case with `dimensions` as `JSON.stringify({dimensions})` which is `{"dimensions":[...]}` not `""`; `src/app/api/analytics/sync/route.ts:126-128` handles `requestedDimensions ? syncAccounts(ids,{dimensions}) : syncAccounts(ids)` — correctly distinguishes bulk with dimensions, but existing `analytics-controls.test.ts` expectations for bulk `body===""` when dimensions undefined still hold; bulk with dimensions now yields non-empty body which previously would have been `""` — test suite passes because no test covers bulk-with-dimensions sync request body assertion; consider adding explicit test for that branch
- API `handleAnalyticsOverviewRequest` validates `dimension` but does not yet route to dimension-specific queries; all dimension SWRs currently receive full overview shape, not filtered `geoRows`; future `queries.ts` extension should respect `?dimension=` and return shape consumed by each chart, otherwise charts will never show real data even after sync captures snapshots
- `updateTab` does not invalidate `syncGeneration`; switching tab does not clear `SyncResults` banner, so stale success from previous tab remains visible; spec keeps `SyncResults` above tabs globally, but tab-specific error banner inside each component's `error` prop already separates per-dimension fetch errors — overall sync feedback stays global by design
